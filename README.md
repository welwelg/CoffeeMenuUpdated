# Coffee Menu Application

A Laravel 12 + React application for managing a coffee menu.

## CI/CD Pipeline

This project uses GitHub Actions for continuous integration and deployment.

- **Tests**: Runs automatically on push to `main` and `develop` branches.
- **Deployment**: Deploys to Render automatically when changes are pushed to `main` AND tests pass.
